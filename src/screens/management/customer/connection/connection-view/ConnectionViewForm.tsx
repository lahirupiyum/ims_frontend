import React from 'react'
import { ConnectionResponse } from '../../../../../types/customer/Connection'

type PropTypes = {
    selectedConnection: ConnectionResponse
}

const ConnectionViewForm = ({selectedConnection}: PropTypes) => {
  return (
    <div>
      
    </div>
  )
}

export default ConnectionViewForm
