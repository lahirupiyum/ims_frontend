export type VendorRequest = {
    name: string,
    email: string,
    phoneNumber: string
}

export type VendorResponse = {
    id: number,
    name:string,
    email: string,
    phoneNumber: string
}