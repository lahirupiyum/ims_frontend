export type VendorResponse = {
    id: number;
    name: string;
    email: string;
    contactNo: string;
}

export type VendorRequest = {
    name: string;
    email: string;
    contactNo: string
}